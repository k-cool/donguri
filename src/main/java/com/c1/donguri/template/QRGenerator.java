package com.c1.donguri.template;import com.google.zxing.BarcodeFormat;import com.google.zxing.client.j2se.MatrixToImageWriter;import com.google.zxing.common.BitMatrix;import com.google.zxing.qrcode.QRCodeWriter;import java.nio.file.FileSystems;import java.nio.file.Path;public class QRGenerator {    public static String generateQR(String url, String path) {        // 파일명이 중복되지 않게 현재 시간(ms)을 붙여서 생성        String fileName = "qr_" + System.currentTimeMillis() + ".png";        try {            QRCodeWriter writer = new QRCodeWriter();            // URL을 200x200 사이즈의 QR 코드로 변환            BitMatrix matrix = writer.encode(url, BarcodeFormat.QR_CODE, 200, 200);            // 서버의 임시 경로(path)에 파일 저장            Path savePath = FileSystems.getDefault().getPath(path, fileName);            MatrixToImageWriter.writeToPath(matrix, "PNG", savePath);            return fileName; // 생성된 파일 이름을 리턴        } catch (Exception e) {            e.printStackTrace();            return null;        }    }}