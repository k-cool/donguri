package com.c1.donguri.template;import com.c1.donguri.util.QRGenerator;import com.c1.donguri.util.S3Uploader;import java.io.*;import javax.servlet.ServletException;import javax.servlet.http.*;import javax.servlet.annotation.*;@WebServlet(name = "qr", value = "/qr")public class QRC extends HttpServlet {    public void doGet(HttpServletRequest request, HttpServletResponse response) throws IOException, ServletException {        String url = request.getParameter("url");        // QR 생성        String path = System.getProperty("user.home")                + File.separator + "Desktop"                + File.separator + "qr";        String prFileName = QRGenerator.QR_GENERATOR.generateQR(url, path);        // S3업로드        S3Uploader s3Uploader = new S3Uploader();        String s3Url = s3Uploader.uploadLocalFile(path, prFileName);        System.out.print("line changed..");        response.setCharacterEncoding("UTF-8");        response.getWriter().print(s3Url);    }    public void destroy() {    }}