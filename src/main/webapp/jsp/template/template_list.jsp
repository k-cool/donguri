<%@ page contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" language="java" %><%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %><% request.setCharacterEncoding("UTF-8"); %> <%-- 한글 깨짐 방지용 --%><link rel="stylesheet" href="css/template_list.css"><title>User's Template List</title><div class="container">    <h2>{ User's Template List }</h2>    <div class="template-grid">        <c:forEach var="t" items="${templateListUser}">            <div class="template-card">                <a href="template-detail?templateId=${t.templateId}">                    <div class="img-wrapper">                        <c:choose>                            <c:when test="${not empty t.coverImgUrl}">                                <img src="${t.coverImgUrl}" alt="${t.name}">                            </c:when>                            <c:otherwise>                                <img src="img/no_image.png" alt="이미지 없음">                            </c:otherwise>                        </c:choose>                    </div>                    <div class="info-box">                        <span class="type-tag">${t.type}</span>                        <h3><strong>${t.name}</strong></h3>                        <div class="date">${t.createdAt}</div>                    </div>                </a>            </div>        </c:forEach>    </div>    <c:if test="${empty templateListUser}">        <div class="empty-msg">            <p>등록된 템플릿 데이터가 없습니다.</p>        </div>    </c:if></div><script>    // TODO: 등록 결과 유저한테 피드백 해주기    // 페이지 로드 시 URL 파라미터를 확인합니다.    // window.onload = function () {    //     const urlParams = new URLSearchParams(window.location.search);    //    //     // 1. 해금 성공 모달    //     if (urlParams.get('unlocked') === 'true') {    //         alert("✨ 새로운 템플릿이 해금되었습니다! ✨");    //         // 주소창에서 파라미터 지우기 (새로고침 시 또 안 뜨게)    //         window.history.replaceState({}, document.title, window.location.pathname);    //     }    //    //     // 2. 이미 보유 중일 때    //     if (urlParams.get('error') === 'already') {    //         alert("이미 보유 중인 템플릿입니다. 😊");    //         window.history.replaceState({}, document.title, window.location.pathname);    //     }    // };</script>